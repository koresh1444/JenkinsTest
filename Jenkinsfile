pipeline {
    agent any
    stage ('Example') {
        steps {
             script {
                 log.info 'Starting'
                 log.warning 'Nothing to do!'
             }
        }
    }
}